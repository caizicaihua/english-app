# 小学双科学习应用 — 当前需求与首版范围

更新日期：2026-09-19。本文描述已实现的二年级双科首版，替代早期仅英语版本的需求说明。实施阶段与剩余验收见 [二年级迭代计划](./GRADE2_ITERATION_PLAN.md)；原暑假方案保留为 [历史计划](./DEVELOPMENT_PLAN.md)。

## 1. 目标与使用方式

为孩子提供少量、可持续的英语和数学练习，让家长按学校正在学习的内容安排课程。保留一至六年级英语内容与自由学习入口，默认设置面向二年级上学期。采用纯前端、本地存储，不要求登录。

日常路径为“设置当前课程 → 今日任务 → 复习和少量新内容 → 完成后结束”。每日 10/15/20 分钟是两科合计的配额设计目标，不保证每个孩子实际用时一致。漏学不生成补课任务；额外练习记录成绩，但须匹配计划要求才完成对应任务。

## 2. 已实现功能

| 模块 | 首版范围 |
|---|---|
| 课程与计划 | 年级、上下学期、英语当前主题、数学知识点、开始日期、每周 3/4/5 天、两科侧重与总时长；教材出版社和版次备注 |
| 今日任务 | 学期模式长期生成；复习优先、减少新增；任务开始后保持当天内容，调整设置不改写既有成绩；区分未开始、休息、计划结束；跨日更新概览 |
| 英语基础 | 一至六年级词库、卡片、例句、对话、语音与语速；选择、听力、拼写、配对、例句和对话填空；所有单元可直接进入 |
| 英语学习闭环 | 单词掌握度、间隔复习、错题、弱单元验证、原有分段诊断、星级与成就 |
| 二年级能力样板 | 家庭、食物、课堂三个主题，每个主题含短句听辨、问答选择与可选家长表达确认；新增情景练习支持同标签页恢复 |
| 数学专项 | 百以内不进位/进位加法、不退位/退位减法、连加减；认识几个几、2–5 与 6–9 的乘法；6/8/10 题，不限时 |
| 数学挑战与错题 | 保留 20 题快速练和 100 题整卷；固定截止时间；知识点独立成绩、错题提示、原题及换数字练习、草稿恢复 |
| 家长反馈 | 新接触词、实际活动、连续天数、英语分项证据、数学知识点表现、周报与下周建议；计划内完成与额外学习分开 |
| 数据保护 | 独立版本与旧数据迁移；存储异常提示与当前页面内存降级；JSON 导出、严格校验恢复、恢复前旧备份、多键失败回滚、两科草稿统一清理 |

英语的表达确认由家长选择记录，不是自动口语评分，也不会直接提高词汇掌握度。没有声音时可改做阅读题，记录阅读证据，不把技术失败判为答错。新分项证据初始为空，不能从旧星级推断听说读写全部掌握。

二年级 138 条词条与例句已做首轮模型语言检查，并更新 11 个单元对话。重大词义修订保留原词条 ID，并安排重新验证。该检查不等于教师、母语者或教材专家复核。

## 3. 数据与统计规则

- 英语进度、数学进度、设置分别保存在三个 `localStorage` key，定义见 `src/utils/storage.ts`、`src/data/bridgePlan.ts` 和 `src/data/math.ts`。
- 普通读取不重复回写；必要迁移独立执行。写入失败时保留当前页面可恢复记录并提醒导出，关闭页面后不保证保留内存数据。
- JSON 备份含格式版本、导出时间及三类持久数据。无效或不支持的版本不得覆盖现有记录；不承诺任意未来版本兼容。草稿存于 `sessionStorage`，不包含在跨设备备份中。
- 重置清除两科学习记录与草稿，由家长明确选择是否保留语速和学习计划设置。
- 学习日期按本地日历计算。任务只能在所属计划内完成，重复完成不重复计数。
- 周报按本地周一至周日统计，截至当前日期；已记录设置历史之后的缺席计划日计入分母，旧数据缺失的历史不凭空推断。
- 最近 180 天保留英语学习会话、技能证据与数学练习历史；累计掌握状态、错题和成绩不因此整体清空。
- 数学快速练和整卷保留各自分数，专项按知识点保存最高正确率；少量样本仅展示记录，不直接判定能力缺陷。

## 4. 页面与技术约束

React 19 + TypeScript 5 + Vite 8，React Router 7、Tailwind CSS 4、Framer Motion，Vitest 测试。页面与年级数据按需加载，路由集中在 `src/App.tsx`。

| 路由 | 用途 |
|---|---|
| `/` | 今日入口与自由探索 |
| `/grade/:gradeId` | 英语单元列表 |
| `/grade/:gradeId/unit/:unitId`、`/grade/:gradeId/quiz/:unitId` | 学词与闯关 |
| `/english/activity/:activityId` | 二年级情景练习 |
| `/bridge`、`/bridge/setup`、`/bridge/today` | 家长空间、课程设置、今日任务 |
| `/bridge/new-words`、`/bridge/daily-quiz` | 每日新词与小练习 |
| `/bridge/review`、`/bridge/verification` | 到期复习与单元验证 |
| `/bridge/diagnostic`、`/bridge/diagnostic/result` | 原有分段诊断与结果 |
| `/bridge/report` | 双科周报 |
| `/math`、`/math/practice`、`/math/result`、`/math/wrong-book` | 数学入口、练习、结果与错题 |
| `/wrong-book`、`/stats`、`/achievements`、`/settings` | 英语错题、统计、成就、设置与备份 |

界面移动端优先，主要按钮与数字键盘须避免被固定导航遮挡。自动化检查使用 `npm test`、`npm run lint`、`npm run build`；音频、触控和恢复仍需实际浏览器验证。

## 5. 未交付与后续验收

- 未按实际出版社、版次逐章映射教材；当前学期标签和教材备注不能称为“教材同步”。
- 三个英语样板不代表所有主题已有完整能力包；旧英语关卡并非全部支持断点恢复。
- 数学未覆盖完整二年级教材，长度、测量、时间、图形、除法和系统生活应用题仍待后续。
- 未实现登录、后端、云同步、多设备自动同步、排行榜、AI 自由对话和自动口语评分。
- 未实现 Service Worker、离线音频或完整 PWA；浏览器本地存储不等于完整离线可用。
- 孩子实际设备的音频、触控和打开方式验收，以及两周家庭试用、每日用时校准尚未完成。测试与开发回归通过不能代替这些验收。
