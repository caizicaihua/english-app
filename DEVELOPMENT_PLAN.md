# 一升二暑假衔接功能开发计划（历史归档）

> 本文保留 2026-07-24 的暑假衔接设计与当时实现记录，不作为当前版本的待办或验收状态。当前范围以 [二年级双科迭代计划](./GRADE2_ITERATION_PLAN.md) 和 [需求文档](./REQUIREMENTS.md) 为准。
>
> 2026-09-19 已实现学期主线、数学专项、英语情景样板与表达确认、备份恢复；教材逐章映射仍未完成。下文的“保持离线”等历史设计目标不代表当前已实现完整离线或 PWA。
>
> 版本：v1.2  
> 制定日期：2026-07-24  
> 修订日期：2026-07-24  
> 目标用户：刚完成小学一年级、暑假后升入二年级的学生及家长

## 当时实现状态（2026-07-24）

第一阶段 MVP 已于 2026-07-24 按里程碑完成：

- M-1：收尾原有学习流与测试基线。
- M0：三类本地存储版本化、逐字段迁移和掌握度状态机。
- M1：六周计划设置、今日任务和二年级前四单元预习队列。
- M2：可跨天恢复的五小节一年级英语诊断与弱单元验证计划。
- M3：普通闯关、错题重练、到期复习和单元验证统一接入掌握度。
- M4：配置驱动的 20 题数学快速练与独立模式成绩。
- M5：按本地周一至周日汇总的家长周报基础版。

截至 2026-07-24，当时仍列为第二阶段的内容包括教材版本映射、专项数学 UI、口头表达确认和学习数据导出；其后进展见新版迭代计划。

## 1. 项目目标

在现有英语学习与一年级口算能力之上，增加一套可持续执行的暑假衔接流程，让家长不需要每天自行安排内容，让孩子在每天 15–20 分钟内完成：

1. 一年级知识诊断与查漏补缺。
2. 一年级薄弱内容的间隔复习。
3. 二年级上学期核心主题的适量预习。
4. 百以内口算的短时、低压力训练。
5. 家长可理解的每周学习反馈。

产品不以“提前学完整个二年级”为目标，而以“基础扎实、保持兴趣、开学后更容易理解”为成功标准。

## 2. 产品原则

- **正确率优先于速度**：先掌握，再提升答题速度。
- **复习优先于新增**：当天待复习内容未完成时，减少新词数量。
- **短时高频**：默认每天 15 分钟，每周 5 天。
- **允许跳过和补做**：不因漏学一天制造连续补课压力。
- **识别、理解、表达并重**：不能只依赖选择题判断掌握。
- **家长看到结论，孩子看到任务**：两套界面表达不同的信息。
- **保持离线和隐私**：继续采用纯前端和本地存储，不引入账号系统。
- **兼容已有数据**：升级后保留当前学习进度、星级、错题和设置。
- **掌握度是当前状态的唯一真相**：错题、诊断薄弱只影响任务优先级，不各自维护一套“是否掌握”状态。
- **历史记录与当前状态分离**：诊断报告保留当时结果，后续是否继续复习由当前掌握度决定。

## 3. 成功指标

首个版本不采集远程分析数据，只在本地计算以下指标：

- 每周完成 4 天及以上学习任务。
- 单次学习时长控制在 10–20 分钟。
- 一年级薄弱单元数量逐周下降。
- 到期复习任务完成率达到 80%。
- 已掌握单词占已接触单词的比例达到 80%。
- 数学快速练习正确率稳定在 90%左右后再提高速度。
- 孩子能够口头使用本周 5–10 个核心词或句型。

## 4. 版本范围

### 4.1 第一阶段：暑假衔接 MVP

必须包含：

1. 一升二计划设置。
2. 一年级综合诊断。
3. 今日学习任务。
4. 自由复习模式，不受课程关卡限制。
5. 单词掌握度和到期复习。
6. 数学 20 题快速练。
7. 家长周报基础版。

### 4.2 第二阶段：内容与体验增强

1. 二年级上、下学期标签。
2. 教材版本映射。
3. 专项数学练习。
4. 口头表达任务和家长确认。
5. 学习数据导出与恢复。

### 4.3 暂不包含

- 登录、云同步和多设备共享。
- 排行榜、同学竞赛和社交功能。
- AI 自动评分或自由对话。
- 强制打卡、付费和广告。
- 未经人工审校的教材内容自动生成。

## 5. 核心用户流程

```text
首页
  → 进入“一升二暑假计划”
  → 选择学习频率与每日时长
  → 分 3–5 次完成一年级诊断
  → 查看诊断结论
  → 生成六周学习安排
  → 每天完成“复习 + 新内容 + 小练习”
  → 周末查看家长周报
  → 根据掌握情况自动调整下一周任务
```

如果用户跳过诊断，系统使用保守方案：从一年级第一单元开始快速复习，并允许随时补做诊断。

## 6. 功能设计

### 6.1 一升二计划设置

新增设置项：

- 当前阶段：固定为“一年级暑假”。
- 计划开始日期。
- 每周学习天数：3、4、5 天。
- 每日时长：10、15、20 分钟。
- 英语与数学侧重：均衡、英语优先、数学优先。
- 是否开启二年级预习。

默认值：

- 每周 5 天。
- 每天 15 分钟。
- 英语与数学均衡。
- 开启二年级前四单元预习。

验收标准：

- 设置可修改且刷新后保留。
- 修改计划不会清空已有学习记录。
- 当天任务可根据新设置重新计算，但已完成任务不回退。

### 6.2 一年级综合诊断

诊断范围覆盖一年级 13 个单元，采用“可分段、自适应、可恢复”的方式：

- 第一轮每单元 2 题，共 26 题。
- 某单元出现错误时，增加 1 道确认题。
- 总题量最多 39 题。
- 默认拆成 5 个小节，每节覆盖 2–3 个单元。
- 每节基础题量 4–6 题，自适应后最多 6–9 题。
- 完成一个小节后即可退出，允许同一天继续下一节，也允许隔天继续。
- 诊断小节计入当天学习任务，不额外要求再完成完整的 15 分钟日常任务。
- 题型以中文选英文、听力和简单拼写为主。
- 诊断不受关卡解锁限制。

单元结论：

| 得分 | 状态 | 后续安排 |
|---|---|---|
| 85–100 | 已掌握 | 进入低频复习 |
| 60–84 | 需要复习 | 一周内安排 2 次 |
| 0–59 | 重点巩固 | 优先安排学习卡和练习 |

诊断结果包含：

- 一年级整体掌握率。
- 已掌握、需复习、重点巩固单元。
- 诊断时的薄弱单词快照。
- 推荐的第一周任务。

单元结论与单词掌握度的桥接规则：

- 只有实际答过的单词才更新掌握度；答错词增加诊断薄弱信号。
- 不能因为一个单元得分低，就把该单元所有未抽测词直接标记为薄弱或降低等级。
- “需要复习”单元从未抽测词中确定性选择 3 个，加入单元待验证计划。
- “重点巩固”单元从未抽测词中确定性选择 5 个，加入单元待验证计划。
- 待验证词不改变原掌握度，每天最多安排 2–3 个；完成实际作答后再更新对应单词的掌握度。
- 第一轮验证正确率低于 80% 时，按每次 2 个继续加入该单元剩余未验证词，直到正确率达标或全部覆盖。
- 单元待验证计划只表示后续任务安排，不表示孩子当前一定不会；完成验证后从计划中移除。
- 待验证词必须按诊断 ID、单元 ID 和单词 ID 确定性选择，保证刷新和恢复结果一致。

验收标准：

- 每个单元至少被考查 2 次。
- 相同单词不会在第一轮重复出现。
- 每完成一题和一个小节都保存进度，刷新后从准确位置继续。
- 可以分多天完成，未完成时仍可使用其他学习功能。
- 诊断错误写入不可变的诊断结果快照，同时更新对应单词的掌握度和优先级信号。
- 弱单元中未抽测的词只进入待验证计划，不提前改变掌握度。
- 后续练习达标后，单词不再进入当前薄弱复习队列，但历史诊断报告保持不变。

### 6.3 今日学习任务

默认 15 分钟任务由两条独立的英语候选管线和一项短练习组成：

1. 复习/强化队列：到期词、当前薄弱词和单元待验证词合计最多 8 个，其中待验证词每天最多 2–3 个。
2. 新词队列：按当前课程与预习计划选择未接触词，最多 5 个。
3. 一次 5–10 题短练习。
4. 每周安排 2–3 次数学快速练。

以下优先级只作用于复习/强化队列内部：

```text
当前答错或诊断薄弱信号
  > 逾期程度
  > 当前低掌握度
  > 重点巩固单元的待验证词
  > 需要复习单元的待验证词
```

规则：

- 复习候选词必须满足以下条件之一：已经接触且到期、具有当前薄弱信号、属于未完成的单元待验证计划。
- 新词候选词必须为 level 0，并按照当前单元、二年级预习顺序和计划名额单独选择。
- 普通 level 0 单词没有 `nextReviewDate`，不会因为“当天到期”混入复习队列。
- 单元待验证词即使暂为 level 0，也消耗复习/强化名额，而不是新词名额。
- 当前掌握判断来自 `WordLearningState`；单元待验证计划只提供任务覆盖范围，不能把未作答词判定为薄弱。
- 不直接拼接 `wrongWords`、`weakWordIds` 等多个状态列表。
- 同一单词当天只出现一次学习任务。
- 待复习内容过多时暂停新增。
- 每日复习有硬上限，历史积压不会一次性全部展示。
- 漏学一天不生成双倍任务。
- 已完成任务在当天保持完成状态。
- 家长可以手动减少任务，不能一键增加到不合理数量。

验收标准：

- 相同输入数据生成相同任务，便于测试和恢复。
- 任务数量符合用户选择的时长。
- 没有内容时提供明确空状态和下一步建议。

### 6.4 单词掌握度与间隔复习

将现有“已学/未学”扩展为五级掌握度：

| 等级 | 名称 | 默认复习间隔 |
|---|---|---|
| 0 | 未接触 | 不进入复习；首次学习后开始计时 |
| 1 | 初次学习 | 1 天 |
| 2 | 需要巩固 | 3 天 |
| 3 | 基本掌握 | 7 天 |
| 4 | 熟练掌握 | 14 天 |

更新规则：

- 正确一次：增加连续正确次数。
- 连续正确两次：掌握度提升一级。
- 答错：连续正确清零，掌握度最多下降一级。
- 拼写和口头任务的权重高于普通选择题。
- 单次闯关不能将整个单元直接设为掌握。
- 无论从诊断、错题本、到期复习还是普通闯关答题，都更新同一份单词掌握度。
- `wrongWords` 不再独立决定当前是否薄弱；达到等级 3 后，当前错题/诊断优先信号统一失效；后续再次答错时重新激活。
- 历史诊断结果和历史学习会话不因掌握度提高而删除。
- 诊断标签、最近答错时间等只作为同等级词之间的排序信号。
- level 0 没有 `nextReviewDate`；只有首次学习、实际诊断或验证作答后才进入间隔复习状态。

验收标准：

- 同一天重复答对不会无限提升等级。
- 答错后会生成下一次复习日期。
- 老版本中已学单词平滑迁移为等级 2，并将首次复习日期确定性地分散到未来 21 天。
- 老版本错题无论是否同时属于已学词，都迁移为等级 1、连续正确数 0，并激活旧错题信号。
- 老版本错题的首次复习日期确定性地分散到未来 7 天，同时受每日复习硬上限保护。
- 同一个词从任意入口练习达标后，所有当前任务入口对其状态保持一致。

### 6.5 二年级上学期预习

第一版直接使用 `src/data/grades/grade2.ts` 中的前四单元：

1. 家庭。
2. 身体。
3. 食物。
4. 衣服。

预习目标：

- 听到单词能够指出对应含义。
- 能够跟读并说出主要单词。
- 能理解简单例句。
- 不要求全部默写或一次通关。

完成一年级重点巩固任务后，才在今日任务中加入二年级新词。每天最多 5 个二年级新词。

注意：当前二年级词库按年级拆分，但尚未绑定特定教材版本。正式标注“二年级上册同步”前，必须确认教材版本并完成人工内容对照。

### 6.6 数学 20 题快速练

MVP 保留现有 100 题模式，并新增 20 题快速练：

| 模式 | 题量 | 时间 | 用途 |
|---|---:|---:|---|
| 每日快速练 | 20 | 5 分钟 | 日常保持手感 |
| 每周测验 | 100 | 15 分钟 | 保留现有比赛模式 |

快速练题目组成：

- 14 道计算。
- 4 道填空。
- 2 道比大小。

生成器在 MVP 中完成配置化，为第二阶段专项练习预留以下单题型配置：

- 20 以内加减。
- 百以内整十与两位数加减。
- 三项连算。
- 填空求未知数。
- 比大小。

专项练习 UI 和专项成绩页属于第二阶段，不作为 MVP 发布阻塞项。

验收标准：

- 所有操作数和结果符合当前范围要求。
- 20 题模式不会写入 100 题模式的最高分。
- 未作答题只有在倒计时结束时计错。
- 用户主动提前交卷时提示未作答数量。
- 错题按题型分类进入数学错题本。

### 6.7 家长周报

周报展示：

- 本周学习天数。
- 完成任务数。
- 新接触、到期复习和已掌握单词数。
- 一年级掌握率变化。
- 二年级预习进度。
- 数学正确率和平均用时。
- 最需要关注的 3 个单元。
- 下周建议。

避免展示：

- 无意义的点击次数。
- 过度强调排名或连续打卡。
- 仅靠学习时长判断效果。

验收标准：

- 无学习记录时显示引导，不出现空图表。
- 周报按本地周一至周日计算。
- 数据全部来自本地学习记录。

## 7. 技术方案

### 7.1 建议新增目录和文件

```text
src/
  data/
    bridgePlan.ts
    curriculum.ts
  pages/
    BridgeSetupPage.tsx
    DiagnosticPage.tsx
    DiagnosticResultPage.tsx
    TodayPlanPage.tsx
    ParentReportPage.tsx
  components/
    DailyTaskCard.tsx
    MasteryBadge.tsx
    UnitMasteryCard.tsx
  utils/
    diagnostic.ts
    mastery.ts
    studyPlan.ts
    weeklyReport.ts
```

### 7.2 建议新增路由

```text
/bridge                 暑假衔接首页
/bridge/setup           计划设置
/bridge/diagnostic      一年级诊断
/bridge/diagnostic/result
/bridge/today           今日任务
/bridge/report          家长周报
```

首个版本先在首页增加“暑假衔接计划”卡片，不立即替换现有首页。

### 7.3 本地存储升级

当前项目使用三个独立的本地存储 key，升级时继续保持职责分离，而不是合并成一个大对象：

| Key | 数据职责 | 升级方式 |
|---|---|---|
| `english_app_data` | 英语进度、掌握度、诊断快照、学习会话 | 独立 `schemaVersion` 和迁移器 |
| `english_app_settings` | 通用设置和暑假计划设置 | 独立 `schemaVersion`、解析器和默认值补全 |
| `english_app_math_data` | 数学成绩、错题和模式记录 | 独立 `schemaVersion` 和迁移器 |

三个 key 均采用 `parse → validate → migrate → normalize` 流程。迁移函数可以由一个存储入口协调，但不能假设三份数据具有相同 schema，也不能继续依赖顶层浅合并补全嵌套字段。

建议的数据结构：

```typescript
interface WordLearningState {
  level: 0 | 1 | 2 | 3 | 4
  correctStreak: number
  wrongCount: number
  lastReviewedAt?: string
  nextReviewDate?: string
  lastCountedCorrectDate?: string
  prioritySignals: {
    legacyWrong?: boolean
    lastWrongAt?: string
    lastDiagnosticWeakAt?: string
  }
}

interface UnitFollowUpPlan {
  unitKey: string
  sourceDiagnosticId: string
  reason: 'review' | 'focus'
  pendingWordIds: string[]
  assessedWordIds: string[]
  correctCount: number
  createdAt: string
}

interface BridgePlanSettings {
  enabled: boolean
  startDate: string
  studyDaysPerWeek: 3 | 4 | 5
  dailyMinutes: 10 | 15 | 20
  focus: 'balanced' | 'english' | 'math'
  previewGrade2: boolean
}

interface DiagnosticResult {
  id: string
  startedAt: string
  completedAt: string
  unitResults: Record<string, {
    score: number
    status: 'mastered' | 'review' | 'focus'
    sampledWordIds: string[]
  }>
  // 历史快照，不作为当前掌握状态的第二真相
  weakWordIds: string[]
}

interface DiagnosticDraft {
  id: string
  startedAt: string
  currentSection: number
  responses: DiagnosticResponse[]
}

interface StudySession {
  id: string
  date: string
  taskType: 'review' | 'new_words' | 'quiz' | 'math'
  itemCount: number
  correctCount: number
  durationSeconds: number
}

type ScoredMathMode = 'paper' | 'quick' | 'focused'

interface MathModeProgress {
  bestScore: number
  lastAttempt: MathAttempt | null
  completedCount: number
}

interface MathProgressDataV2 {
  modeProgress: Partial<Record<ScoredMathMode, MathModeProgress>>
  // 用于结果页显示最近一次作答，review 也可以写入
  latestAttempt: MathAttempt | null
  wrongQuestions: MathWrongQuestion[]
}
```

兼容策略：

- 三个 key 分别增加版本号、迁移函数和逐字段规范化函数。
- `WordLearningState` 是单词当前能力状态和间隔复习日期的唯一真相。
- `DiagnosticResult.weakWordIds` 仅表示诊断完成时的历史快照，不直接作为可变任务列表。
- `UnitFollowUpPlan` 只跟踪弱单元中未抽测词的覆盖任务，不改变这些词的掌握度；实际作答后才写入 `WordLearningState`。
- 待验证词按稳定规则选择；计划恢复、重复生成和跨天继续不得更换候选词。
- 仅存在于旧 `learnedWords` 的词映射为等级 2；首次到期日按 `hash(wordId + schemaVersion) % 21` 分散到未来 21 天。
- 存在于旧 `wrongWords` 的词优先按错误证据迁移：无论是否同时在 `learnedWords`，均映射为等级 1、`correctStreak = 0`，首次到期日按稳定哈希分散到未来 7 天。
- 既不在 `learnedWords` 也不在 `wrongWords` 的词不创建持久化掌握记录，视为隐式 level 0，且没有到期日。
- 错峰算法必须确定且幂等，迁移重复执行不得改变到期日或制造重复记录。
- 旧的 `wrongWords` 转换为掌握度内的优先级信号；兼容期如果保留旧字段，只能作为新状态的投影，不允许两边独立写入。
- 任意入口答题后都只调用同一个掌握度状态机；达到等级 3 后清除当前优先信号，后续再次答错时重新激活，但始终保留历史诊断和学习记录。
- 对数组、日期、枚举和嵌套对象逐字段校验；单个字段异常时修复该字段，不清空整份存储。
- 加载旧数据、迁移新数据和保存回写应分别测试。
- 数学旧 `bestScore` 和 `lastAttempt` 迁移到 `modeProgress.paper`；`latestAttempt` 保持结果页语义。
- `quick`、`paper`、后续的 `focused` 分别保存最高分和最近成绩；`review` 不记录最高分，只允许更新全局 `latestAttempt` 和错题状态。
- `StudySession` 只保留最近 180 天，避免 `localStorage` 无限增长。

### 7.4 数学生成器配置化与可测试化

当前生成器将 35/20/15 道计算、20 道填空和 10 道比大小写死，配置只在生成结束后校验总数。新增快速练前必须先完成生成器重构：

- 每类题量由配置驱动，而不是固定循环上限。
- 计算题内部的小数运算、整十/两位数运算、连算比例也由配置控制。
- `MathPracticeMode` 扩展为至少 `'paper' | 'quick' | 'review'`，第二阶段再加入 `'focused'`。
- 试卷、作答记录和题型统计保存本次实际配置，不能引用全局 100 题配置。
- 抽出单题型生成入口，为第二阶段专项练习复用。
- 生成器接收可选随机源：

```typescript
type RandomSource = () => number
```

生产环境使用 `Math.random`，测试环境使用固定种子，避免随机测试偶发失败。

## 8. 开发里程碑

以下为单人开发的相对估算，不包含教材内容的大规模人工审校。

### M-1：当前工作区收尾（0.5–1 天）

- 复核当前未提交的功能修复和新增测试。
- 运行现有 `test`、`lint`、`build`。
- 将当前修复、测试和文档整理为边界清晰的基线提交。
- 排除无关文件（例如未确认用途的 `403.md`），避免混入迁移改动。
- 在基线稳定后再开始存储 schema 调整。

完成标准：后续迁移改动可以与当前 WIP 清晰区分，并能随时回到稳定基线。

### M0：学习状态核心与存储迁移（4–5 天）

- 确定“当前掌握度唯一真相、诊断结果保留历史快照”的状态边界。
- 实现掌握度状态机、优先级信号和统一更新入口。
- 定义单元待验证计划，明确“未抽测”与“答错”的数据差异。
- 为三个存储 key 分别添加版本、迁移和逐字段规范化。
- 实现旧 `learnedWords`、`wrongWords` 的兼容迁移。
- 使用稳定哈希将旧已学词到期日分散到未来 21 天。
- 将旧数学成绩迁移到按模式存储的 `modeProgress.paper`。
- 为旧数据、嵌套字段损坏和重复迁移补测试。
- 为日期边界和数据损坏补测试。

完成标准：现有用户升级后数据完整；重复迁移结果一致；不会一次产生大量到期任务；任意入口调用同一个状态机。

### M1：计划设置与今日任务骨架（2–3 天）

- 完成计划设置页。
- 完成暑假衔接首页。
- 实现每日任务生成器。
- 接入首页入口。

完成标准：可生成并保存一份六周计划，今日任务刷新后保持一致。

### M2：分段式一年级诊断（4–5 天）

- 实现跨单元选题。
- 实现 3–5 次小节式诊断、逐题保存和跨天恢复。
- 实现单元评分和结果页。
- 将历史结果快照和当前掌握度分别写入正确位置。
- 为需要复习和重点巩固单元生成确定性的待验证计划。
- 根据验证正确率扩展或结束单元覆盖任务。
- 将诊断优先级信号接入每日任务。

完成标准：孩子每次只需完成 4–9 题；总计 26–39 题后能得到可解释的薄弱单元和第一周安排；未抽测词不会被提前判错，但弱单元能够持续产生受限的验证任务。

### M3：间隔复习与现有学习入口接入（4–6 天）

- 接入英语闯关和错题重练。
- 实现到期复习列表。
- 在学习卡和错题本展示掌握状态。
- 统一诊断、错题、到期复习和普通闯关的状态更新。
- 验证达标后所有当前任务入口同步回收，历史报告不被改写。

完成标准：正确、错误和跨天复习都会按规则更新下一次复习日期。

### M4：数学生成器重构与快速练（4–6 天）

- 将各题型和计算子类型题量改为配置驱动。
- 注入随机源并为各配置组合补测试。
- 让作答记录和题型统计使用试卷实际配置。
- 支持 20 题快速练配置。
- 增加模式选择和提前交卷提示。
- 增加题型统计。
- 区分各模式成绩。
- 抽出专项题型生成能力，专项 UI 留到第二阶段。

完成标准：20 题模式可在手机上顺畅完成，成绩和错题不污染 100 题记录。

### M5：家长周报与整体打磨（2–3 天）

- 汇总最近一周学习记录。
- 展示薄弱内容和下周建议。
- 完成空状态、错误状态和移动端回归。
- 更新 README 和需求文档。

完成标准：家长能在一分钟内判断孩子本周学了什么、哪里薄弱、下周做什么。

预计总工期：20.5–29 个有效开发日。若第二阶段同时交付专项数学 UI，另增加约 1–2 天。

## 9. 测试计划

### 9.1 单元测试

- 旧数据到新数据的迁移。
- 三个存储 key 的独立版本、字段规范化和损坏恢复。
- 迁移重复执行的幂等性，以及旧已学词 21 天错峰分布。
- 旧错题覆盖旧已学状态、迁移为等级 1，并在 7 天内稳定错峰。
- 不在旧已学/错题集合中的词保持隐式 level 0 且没有到期日。
- 本地日期、跨月、跨年和连续天数。
- 掌握度升级、降级和同日去重。
- 从诊断、错题、复习、闯关进入时更新同一掌握状态。
- 当前薄弱信号回收后，历史诊断快照保持不变。
- 到期复习日期计算。
- 今日任务数量、优先级和去重。
- 复习/强化与新词候选池相互独立，普通 level 0 不进入复习池。
- 诊断分节、跨天恢复、题目覆盖率和最大题量。
- 弱单元待验证词确定性选择、每日限额、低于 80% 时扩展以及完成回收。
- 未抽测词进入待验证计划时不改变掌握度。
- 数学各模式的题量、范围和计分。
- 旧数学最高分迁移到 `paper`，各模式成绩相互隔离，`review` 不产生最高分。
- 数学配置驱动的题型分布、固定随机种子和实际配置统计。
- 词库 ID、目录和例句一致性。

### 9.2 页面流程测试

- 首次进入设置计划。
- 跳过诊断后的默认计划。
- 诊断中途刷新、完成小节后退出和隔天继续。
- 完成今日任务后首页状态更新。
- 错题进入第二天复习。
- 20 题数学练习提交与重练。
- 无记录时打开家长周报。

### 9.3 浏览器回归

- 390 × 844 手机视口。
- 768px 平板视口。
- 1280px 桌面视口。
- 无横向溢出。
- 底部导航不遮挡主要按钮。
- 刷新任意 Hash Route 后页面可恢复。
- Web Speech API 不可用时页面仍可学习。

### 9.4 每个里程碑的质量门槛

```bash
npm test
npm run lint
npm run build
```

以上三项是硬门槛，全部通过后才能进入下一里程碑。

每个里程碑同时运行依赖安全审计并记录结果。审计告警需要评估是否影响实际发布面；未解决且影响发布面的高危或严重漏洞是发布阻塞项，但不因无关的上游或开发依赖告警机械阻塞每个里程碑。

## 10. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 内容与学校教材不一致 | 预习价值下降 | 明确使用二年级分级词库，未确认教材前不称“教材同步” |
| 每日任务过多 | 孩子抵触 | 严格按时长限制，新内容最多 5 个词 |
| 旧已学词集中到期 | 升级后出现复习洪峰 | 稳定哈希错峰 21 天，并设置每日复习硬上限 |
| 多套薄弱状态漂移 | 同一单词反复出现或状态矛盾 | 掌握度作为唯一当前状态，其他数据仅作为历史或排序信号 |
| 少量抽测被过度推断 | 未抽测词被错误标记为不会 | 弱单元创建受限的待验证计划，实际作答后才更新掌握度 |
| 只会选择不会表达 | 虚假掌握 | 增加拼写权重和家长确认的口头任务 |
| 随机题导致结果不稳定 | 测试和诊断不可信 | 使用可注入随机源和诊断固定题池 |
| 本地数据损坏或丢失 | 长期记录消失 | 三个 key 独立迁移并逐字段规范化，第二阶段增加导出与恢复 |
| 诊断一次过长 | 首次体验像考试、孩子中途放弃 | 拆成 3–5 个小节，逐题保存并允许跨天完成 |
| 语音浏览器兼容差 | 听力题不可用 | 提供重播、文字替代和能力检测 |
| 家长过度关注打卡 | 增加压力 | 周报强调掌握和薄弱内容，不做排名 |

## 11. 推荐实施顺序

严格按以下顺序执行：

1. M-1 收尾并提交当前工作区基线。
2. M0 先完成掌握度状态机、存储边界和迁移。
3. M1 计划设置和今日任务骨架。
4. M2 在统一学习状态之上实现分段诊断。
5. M3 将间隔复习接入全部现有学习入口。
6. M4 先重构数学生成器，再发布 20 题快速练。
7. M5 家长周报。
8. 第二阶段再做专项数学 UI。
9. 确认教材版本后再做二年级上册精确映射。

不建议先做漂亮的周报或复杂动画。没有可靠的诊断、掌握度和学习记录时，周报只能展示不可信的数据。

## 12. Definition of Done

一个功能只有同时满足以下条件才算完成：

- 有明确的儿童端和家长端使用路径。
- 刷新和重新打开应用后状态正确。
- 不破坏旧版 `localStorage` 数据。
- 有正常、空数据和异常数据状态。
- 核心业务逻辑有自动化测试。
- 390px 手机端可完整操作。
- `test`、`lint`、`build` 通过。
- 依赖安全审计已记录并评估，不存在未解决且影响发布面的高危或严重漏洞。
- README、需求文档和数据结构说明同步更新。
