# 二年级双科首版交付与自检

日期：2026-09-19。当前是本地开发交付，未发布到线上。

## 已实现

- 长期学期计划：家长选择年级、当前英语单元、数学知识点、每周学习日和两科总时长；开始后的任务冻结，后续改课不替换正在做的内容。
- 英语：开放单元选择，首轮模型检查二年级 138 词及例句、更新 11 单元对话；新增家庭、食物、课堂活动，保留已有词汇题型。语音不可用时改为认读题，表达仅记录可选的家长确认。
- 数学：8 个专项知识点、乘法阵列图、解题提示、换数字重练；日常不限时，保留计时挑战。按知识点记录成绩，支持暂停和草稿恢复。
- 双科反馈：分开新词、英语作答、数学作答；技能数据来自实际练习；周报计入已记录设置下未打开应用的学习日，排除未来日期和额外练习对完成率的影响。
- 数据可靠性：旧数据迁移、内容修正后的掌握度复核、写入失败提示与内存暂存、JSON 备份校验与恢复、失败回滚、统一清理两科草稿。

## 浏览器回归

使用独立本地端口 `127.0.0.1:5197` 的自检数据，未重置用户原有来源中的学习记录。

| 流程 | 实际检查结果 |
|---|---|
| 首页与设置 | 首次无记录可正常进入；设置二年级食物主题和乘法认识后，首页正确展示，刷新可保留 |
| 今日安排 | 自检日期为周六，正确显示休息日；工作日、未开始、六周之后等状态通过自动化测试覆盖 |
| 自由学习 | 二年级各单元可直接进入，三个活动入口可见 |
| 英语活动 | 完成 6 题、故意答错 1 题，结果为 5/6；刷新保持已答题反馈；中途离开已有证据计入周报；未确认口头表达时不生成表达能力记录 |
| 语音降级 | 无声音时可切为文字理解，证据归入认读，不伪装为听辨成绩；未核验物理扬声器和每种系统语音 |
| 数学草稿 | 输入 2 题后刷新，保留相同题目与答案，并进入暂停状态 |
| 数学错题闭环 | 6 题答对 5 题显示 83%；错题有对应解释；换数字后答对，原错题从待复习列表移除 |
| 周报 | 正确显示英语 5/6、数学两次合计 6/7；休息日额外学习不增加计划完成率 |
| 响应式 | 检查 390×844 手机、768×1024 平板与 1280×800 桌面布局；修复手机键盘遮挡，复验首题、5 排阵列和末题完整可见；平板和桌面未发现横向溢出 |
| 备份界面 | 导出按钮调用无控制台错误；下载到文件系统及用户文件选择器恢复尚未完成实机端到端确认。格式、往返恢复、损坏拒绝及失败回滚有自动化测试 |

## 最终工程检查

- `npm run test`：20 个测试文件、139 项测试全部通过。
- `npm run lint`：通过。
- `npm run build`：TypeScript 检查与生产构建通过。
- `npm run preview`：生产首页、统计和周报加载及导航正常，当前浏览器检查未出现控制台错误；统计显示英语作答 6 次、数学作答 7 次，与自检流程相符。
- `git diff --check`：通过。
- React Router 已在现有主版本内更新至 7.18.4。
- `npm audit --omit=dev`：生产依赖 0 项告警。
- `npm audit`：仍有 7 项开发依赖告警（3 high、4 moderate），涉及 `@humanfs/node`、`@vitest/mocker`、`vitest`、`brace-expansion`、`js-yaml`、`nanoid`、`postcss`。

剩余审计修复没有完成。npm 自动修复、明确补丁安装、临时 npm，以及不带 `node_modules` 的隔离锁文件修复均遇到 Arborist `edgesOut` 空指针异常；未使用强制安装、跳过 peer 校验或跨主版本升级。保留通过工程检查的稳定依赖，需要后续解决锁文件/解析器兼容问题后继续修补。生产依赖审计为零不代表整个开发工具链已经无风险。

## 自检中追加修复

- 今日计划做到一半时修改设置，保留原任务；设置历史从正确日期生效。
- 英语逐题作答即时计入统计，完整提交后不重复计数。
- 英语、新词和数学会话绑定原任务日期，跨午夜及恢复草稿不会勾选下一天任务。
- 数学题卡在键盘展开、再次聚焦和窗口尺寸改变时重新定位。
- 备份拒绝不一致的数学分数、题量、答案判断和未知数据版本。

## 仍需家庭试用确认

- 孩子使用的教材版本、当前章节和已有乘法基础尚未提供；目前是通用能力练习。
- 内容完成模型首轮语言检查，未经过教师或母语者复核；新增完整情景活动仅覆盖三个主题。
- 数学尚不包含长度、时间、图形、除法及完整的生活应用题体系。
- 每天 10/15/20 分钟是题量设计目标，需要实际试用校准。需要在孩子常用设备验证语音、触控和文件备份，并进行两周使用反馈。
- 草稿保存在当前标签页；旧英语关卡未全部支持断点恢复。没有账号、云同步、自动口语评分或完整 PWA 离线支持。

完整范围与后续路线见 [迭代计划](./GRADE2_ITERATION_PLAN.md)。
